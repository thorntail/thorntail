swarm:
  deployment:
    test.war:
      web:
        login-config:
          auth-method: KEYCLOAK
        security-constraints:
          - url-pattern: /secured
            methods: [GET]
            roles: [admin]
  keycloak:
    multitenancy:
      paths:
        /secured: keycloakTenant.json
