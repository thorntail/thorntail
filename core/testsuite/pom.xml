<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>io.thorntail</groupId>
    <artifactId>thorntail-core-parent</artifactId>
    <version>1.0.0-SNAPSHOT</version>
  </parent>

  <groupId>io.thorntail.testsuite</groupId>
  <artifactId>thorntail-testsuite-parent</artifactId>
  <version>1.0.0-SNAPSHOT</version>

  <name>Testsuite Parent</name>

  <packaging>pom</packaging>

  <dependencies>
    <dependency>
      <groupId>io.thorntail</groupId>
      <artifactId>thorntail-testing</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-failsafe-plugin</artifactId>
        <executions>
          <execution>
            <id>integration-test</id>
            <phase>integration-test</phase>
            <goals>
              <goal>integration-test</goal>
            </goals>
            <configuration>
              <testSourceDirectory>src/it/java</testSourceDirectory>
              <skip>${skipTests}</skip>
            </configuration>
          </execution>
          <execution>
            <id>verify-integration-test</id>
            <phase>verify</phase>
            <goals>
              <goal>verify</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>io.fabric8</groupId>
        <artifactId>docker-maven-plugin</artifactId>
        <version>${version.docker-maven-plugin}</version>
        <configuration>
          <skip>${skipTests}</skip>
        </configuration>
        <executions>
          <execution>
            <id>start</id>
            <phase>pre-integration-test</phase>
            <goals>
              <goal>start</goal>
            </goals>
          </execution>
          <execution>
            <id>stop</id>
            <phase>post-integration-test</phase>
            <goals>
              <goal>stop</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <modules>
    <module>config</module>
    <module>bean-validation</module>
    <module>servlet</module>
    <module>servlet-static</module>
    <module>jaxrs</module>
    <module>jaxrs-jsonp</module>
    <module>jms</module>
    <module>jms-opentracing</module>

    <module>jpa</module>
    <module>jpa-jta</module>
    <module>jpa-opentracing</module>

    <module>opentracing-jaeger</module>

    <module>openapi</module>

    <module>vertx</module>
    <module>websockets</module>

    <module>keycloak</module>
    <module>proof</module>

    <module>docker-thin-layers</module>
    <module>docker-fabric8</module>
  </modules>
</project>
